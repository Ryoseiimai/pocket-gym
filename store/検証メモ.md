# 申請素材の検証 — 2026-09-23

- `node --test app/tests/*.test.js`：52件成功、失敗0。
- `bash .github/scripts/static-checks.sh .`：成功。
- `sample-state.json`：本番とローカル双方の `validateImport` で成功、検証前後のデータが完全一致。
- スクリーンショット5枚：1320×2868、RGB PNG。目視で日本語・ボタン・下部タブの重なり、横はみ出しがないことを確認。振り返りの長い一覧は実画面どおり縦スクロールする。
- 本番撮影中のJavaScript・コンソールエラー：0。条件とブラウザ版は `capture-info.json`。
- 本番のSVGにはローカル版と異なり `.level-chart` クラスがないため、撮影スクリプトは `.chart-list svg` の描画を待つ。アプリのDOM・CSSは撮影時も変更していない。
- `privacy.html`：LPとCSP完全一致、既存の `lp.css` を利用。ローカル配信でフッターリンクの遷移、ライト・ダーク両方の横はみ出しなし、コンソールエラー0。ライト表示を目視確認。
- ASC文字数：App名6字、サブタイトル20字、プロモーション122字、キーワード57字。
- `.github/`・`autopilot/`・`app/js/` は変更していない。
- 公開・push・ASCへの登録や提出は行っていない。
